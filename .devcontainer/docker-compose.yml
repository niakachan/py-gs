version: "3.8"
services:
  py-gs-dev:
    build: ./
    tty: true
    working_dir: /opt/py-gs/
    container_name: py-gs-dev
    volumes:
      - ..:/opt/py-gs:delegated
      - ~/.ssh:/root/.ssh:delegated
